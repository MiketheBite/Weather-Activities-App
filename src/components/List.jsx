
export default function List({activities}){
    return <>
    <ul>
        {activities.map((activity) => {
            return (
                <li>

                </li>
            )
        })}
    </ul>
    </>
}